function syncMarker(latLng) {
  console.log($('#map-canvas').data())
  $.ajax({
    method: "POST",
    url: "http://localhost:3000/waypoints",
    data: {latitude: latLng.k, longitude: latLng.D, journey: $('#map-canvas').data("journeyid")}
  })
}