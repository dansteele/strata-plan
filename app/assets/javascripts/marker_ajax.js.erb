JourneyMap.prototype.syncMarker = function(latLng) {
  var _this = this;
  $.ajax({
    parent: _this,
    method: "POST",
    url: "http://strata-plan.herokuapp.com/waypoints", // TODO: Make dynamic 
    data: {latitude: latLng.F, longitude: latLng.A, journey: $('#map-canvas').data("journeyid")},
    success: function(res) {
      $('.waypoint-list').html(res);
      addDeleteListener();
      this.parent.updateRoute(res)
    }
  })
}